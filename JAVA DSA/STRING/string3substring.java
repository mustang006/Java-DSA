package STRING;

public class string3substring {
    public static void main(String[] args) {
        String sentence="TonyStar";
        String name=sentence.substring(4,sentence.length());
        System.out.println(name);
    }
    //sentence,substring(beginindex,defaultindex)
    //default index is the length of that string i.e sentence.lengthl
}
